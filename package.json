{
  "name": "youtube-to-spotify",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "clean": "rimraf dist",
    "build:extension": "npm run clean && npm run build && node scripts/post-build.js"
  },
  "dependencies": {
    "vue": "^3.3.4"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^4.2.3",
    "rimraf": "^5.0.1",
    "rollup-plugin-copy": "^3.5.0",
    "vite": "^4.4.5"
  }
}
